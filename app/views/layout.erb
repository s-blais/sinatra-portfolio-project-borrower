<!doctype html>
<html>
  <head>
    <title>Borrower</title>
    <link rel="stylesheet" type="text/css" href="<%= url("/stylesheets/main.css") %>">
  </head>
  <body>
    <h1>Borrower</h1>
    <%= yield %>
    <% if logged_in? %>
      <footer>
          <br><br>
          <a class="button" href="/items">all Borrower items</a>
          <a class="button" href="/users/<%= current_user.id %>">my info & my items</a>
          <br>
          <a class="button" href="/logout">log out <%= current_user.username %></a>
      </footer>
    <% end %>
  </body>
</html>